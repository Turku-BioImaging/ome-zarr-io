[tool:pytest]
markers =
    gpu: marks tests as requiring GPU/CUDA hardware (deselect with '-m "not gpu"')

# Default test configuration
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage configuration
addopts = --strict-markers
