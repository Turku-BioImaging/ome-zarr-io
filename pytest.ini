[pytest]
# Default test configuration
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage configuration
addopts = --strict-markers
filterwarnings =
    error
    # tests still cover the deprecated Omero / omero_metadata path
    ignore:.*is deprecated; describe channels:DeprecationWarning
